
use mr_clean::entities::room::RoomEntity;
use mr_clean::repositories::roomrepository::RoomRepository;
use mr_clean::repositories::inmemoryroomrepository::InMemoryRoomRepository;

fn main() {

    let repo = InMemoryRoomRepository::new();
    
}
